.env
.venv/
venv/
__pycache__/
*.pyc
*.db
*.sqlite
.DS_Store
backtest_results/

# Phase 4 Learning Loop
# (inline-kommentarer er IKKE tilladt i .gitignore — mønstret skal stå alene på linjen)
data/chromadb/
reflection/audit.log
reflection/reports/
config.yaml.bak.*

# DB-backups taget før Alembic-migrationer (samme mønster som config-backups)
trading_bot.db.bak.*

# Phase 5 — dashboard runtime-status (skrives af status_writer.py)
bot_status.json

# Runtime-logs (skrives af LaunchAgent/cron — aldrig commit)
bot.log
bot_error.log

# Historiske markedsdata - store filer, hentes/genskabes, aldrig i git
data/historical_xau/*.csv
data/historical_xau/*.csv.gz
data/historical/
# Optagede orderbook-snapshots: vokser med ~0,6 GB/aar.
# README'en er versionsstyret, dataen er ikke.
data/orderbook/*/
data/orderbook/_*
research/output/*.csv

.claude/
